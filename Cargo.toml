[package]
name = "lta"
version = "0.3.0"
authors = ["budinverse <budisyahiddin@gmail.com>"]
edition = "2018"
homepage = "https://github.com/BudiNverse/lta-rs"
repository = "https://github.com/BudiNverse/lta-rs"
description = "🚍 Singapore LTA Datamall Rust Client written in pure rust with support for asynchronous requests"
readme = "README.md"
license = "MIT"
keywords = ["LTA", "Singapore", "Client", "Datamall"]
exclude = ["/.travis.yml", "/logo.png", "./azure-pipelines.yml", "./dumped_data", "./lta_models/benches"]

[lib]
name = "lta"

[features]
async = ["lta_async"]
blocking = ["lta_blocking"]

[dependencies]
lta_async = { path = "lta_async", optional = true }
lta_blocking = { path = "lta_blocking", optional = true }
lta_models = { version = "0.1.0", optional = false }
lta_utils_commons = { version = "0.1.0", optional = false }

[dev-dependencies]
tokio = { version = "0.2.6", features = ["macros"]}

[workspace]
members = [
    "lta_async",
    "lta_models",
    "lta_blocking",
    "lta_utils_commons",
]
